spring:
  application:
    name: discovery-server-micro-service
  cloud:
    config:
      enabled: false  # Not fetching config from Config Server
  eureka:
    client:
      register-with-eureka: false
      fetch-registry: false
server:
  port: 8083
	
	spring:
	  cloud:
	    config:
	      uri: http://localhost:8888
	      import: configserver:
	    retry:
	      initial-interval: 3000
	      max-attempts: 3
