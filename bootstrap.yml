spring:
  application:
    name: home-service
  cloud:
    config:
      uri: http://localhost:8888
      import: configserver:
    discovery:
      client:
        service-url:
          default-zone: http://localhost:8761/eureka/
